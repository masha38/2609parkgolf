# 위해 파크골프 상담 신청 Apps Script 연동 명세서

## 1. 목적

랜딩페이지의 상담 신청 내용을 Google Apps Script 웹 앱으로 받아 구글시트 `상담신청` 탭에 저장한다. 폼에는 희망 투어, 이름, 연락처, 개인정보 동의만 노출하고 광고 성과 분석용 UTM 값과 접속 정보는 자동으로 함께 전송한다.

## 2. 전체 구성

1. 고객이 `index.html`의 상담 신청 폼을 작성한다.
2. `script.js`가 입력값과 유입 정보를 JSON으로 구성한다.
3. Apps Script 웹 앱 `/exec` 주소로 `POST` 요청을 보낸다.
4. `Code.gs`가 필수값, 휴대전화 번호, 상품 코드, 개인정보 동의를 검증한다.
5. 정상 요청을 구글시트 `상담신청` 탭의 다음 빈 행에 저장한다.
6. 같은 연락처와 상품으로 2분 안에 반복된 요청은 중복 신청으로 처리한다.

## 3. 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 실제 상담 폼과 숨김 유입정보 필드 |
| `script.js` | 입력 검증, UTM 수집, Apps Script 전송 |
| `config.js` | Apps Script 웹 앱 URL 설정 |
| `apps-script/Code.gs` | 폼 수신, 서버 검증, 구글시트 저장 |
| `apps-script/appsscript.json` | Apps Script 시간대와 런타임 설정 |
| `구글시트_상담신청_컬럼샘플.xlsx` | 상담신청·컬럼정의·코드값 탭 샘플 |

## 4. 폼 요청 명세

### 전송 주소

Apps Script를 웹 앱으로 배포한 뒤 받은 `https://script.google.com/macros/s/.../exec` 주소를 `config.js`의 `formEndpoint`에 입력한다.

### 전송 방식

- Method: `POST`
- Content-Type: `text/plain;charset=utf-8`
- Body: JSON 문자열
- 브라우저 모드: `no-cors`

Apps Script 웹 앱과 랜딩페이지의 도메인이 다르므로 브라우저가 응답 본문을 읽지 않는 단순 요청 방식으로 전송한다. 화면의 성공 문구는 전송 요청이 완료되었다는 뜻이며 최종 저장 여부는 구글시트와 Apps Script 실행 기록에서 확인한다.

### 요청 필드

| 키 | 필수 | 예시 | 설명 |
|---|---:|---|---|
| `tour` | Y | `air` | `cruise`, `air`, `compare` 중 하나 |
| `name` | Y | `홍길동` | 신청자 이름, 최대 30자 |
| `phone` | Y | `010-1234-5678` | 국내 휴대전화 번호 |
| `privacy` | Y | `on` | 개인정보 수집·이용 동의 |
| `utm_source` | N | `naver` | 유입 플랫폼 |
| `utm_medium` | N | `cpc` | 광고 매체 유형 |
| `utm_campaign` | N | `weihai_park_golf` | 캠페인명 |
| `utm_content` | N | `hero_a` | 광고 소재·버튼 구분 |
| `utm_term` | N | `중국 파크골프` | 검색 키워드 |
| `page_url` | 자동 | 랜딩페이지 URL | 신청 페이지 주소 |
| `referrer` | 자동 | 이전 페이지 URL | 직전 유입 페이지 |
| `user_agent` | 자동 | 브라우저 정보 | 사용자 환경 확인용 |
| `website` | 자동 | 빈 값 | 자동입력 방지용 허니팟 |

### 상품 코드

| 코드 | 저장 상품명 |
|---|---|
| `cruise` | 9월 1일 크루즈 파크골프 4박 5일 |
| `air` | 9월 14일 항공 파크골프 3박 4일 |
| `compare` | 두 상품 비교 상담 |

## 5. 구글시트 저장 명세

시트명은 `상담신청`, 헤더는 4행, 실제 접수 데이터는 5행부터 저장한다.

| 순서 | 컬럼 | 작성 주체 | 설명 |
|---:|---|---|---|
| 1 | 접수일시 | 서버 | Apps Script가 접수한 한국 시간 |
| 2 | 리드ID | 서버 | UUID 고유 식별자 |
| 3 | 상태 | 서버/담당자 | 신규, 상담중, 예약완료, 보류, 취소 |
| 4 | 희망상품코드 | 폼 | `cruise`, `air`, `compare` |
| 5 | 희망상품명 | 서버 | 상품 코드에 대응하는 표시명 |
| 6 | 이름 | 폼 | 상담 신청자 이름 |
| 7 | 연락처 | 폼 | 하이픈을 포함한 텍스트 형식 |
| 8 | 개인정보동의 | 폼 | 정상 접수 시 `TRUE` |
| 9~13 | UTM 5개 컬럼 | 폼 | 광고 유입 분석값 |
| 14 | 페이지URL | 폼 | 신청 페이지 주소 |
| 15 | 이전페이지 | 폼 | 직전 유입 페이지 |
| 16 | 사용자환경 | 폼 | 브라우저 user agent |
| 17 | 처리담당자 | 담당자 | 상담 담당자 이름 |
| 18 | 상담결과 | 담당자 | 통화·예약 결과 |
| 19 | 메모 | 담당자 | 객실, 동행, 재통화 등 상담 메모 |
| 20 | 최종수정일 | 서버/담당자 | 마지막 처리 일시 |

## 6. 설치 및 배포 절차

1. 구글 드라이브에서 `구글시트_상담신청_컬럼샘플.xlsx`를 업로드하고 구글시트로 연다.
2. `파일 > Google 스프레드시트로 저장`을 선택해 구글시트 형식으로 변환한다.
3. `확장 프로그램 > Apps Script`를 연다.
4. 기본 `Code.gs` 내용을 프로젝트의 `apps-script/Code.gs` 내용으로 교체한다.
5. 프로젝트 설정에서 시간대를 `Asia/Seoul`로 설정한다.
6. 함수 목록에서 `setupProject`를 선택해 한 번 실행하고 권한을 승인한다.
7. `배포 > 새 배포 > 유형 선택 > 웹 앱`을 선택한다.
8. 실행 사용자는 `나`, 액세스 사용자는 `모든 사용자`로 설정해 배포한다.
9. 발급된 `/exec` URL을 `config.js`의 `formEndpoint`에 입력한다.
10. 랜딩페이지에서 테스트 신청 후 `상담신청` 탭과 Apps Script `실행` 기록을 확인한다.

코드를 변경한 경우에는 기존 배포를 편집해 새 버전으로 다시 배포해야 수정 내용이 실제 `/exec` 주소에 반영된다.

## 7. 검증 및 보호 규칙

- 허용된 상품 코드만 저장한다.
- 이름은 두 글자 이상, 최대 30자로 제한한다.
- 국내 휴대전화 형식을 서버에서 다시 검증한다.
- 개인정보 동의가 없는 요청은 저장하지 않는다.
- 화면에 보이지 않는 `website` 필드에 값이 들어오면 자동입력으로 간주해 저장하지 않는다.
- 같은 연락처와 상품의 2분 이내 반복 신청을 막는다.
- 모든 문자열에 최대 길이를 적용하고 제어문자를 제거한다.
- 동시 신청 시 행 충돌을 방지하기 위해 Script Lock을 사용한다.

## 8. 운영 권장사항

- 구글시트 접근 권한은 상담 담당자에게만 부여한다.
- 구글시트 자체를 웹에 공개하지 않는다.
- 연락처 다운로드와 외부 공유를 최소화한다.
- 개인정보 보유기간이 끝난 상담 데이터는 운영 정책에 따라 삭제한다.
- 월 1회 이상 Apps Script 실행 오류와 중복·스팸 접수 여부를 확인한다.
- 광고별 성과는 `UTM소스`, `UTM매체`, `UTM캠페인`, `UTM콘텐츠`, `UTM키워드`로 구분한다.

## 9. 테스트 체크리스트

- 크루즈, 항공, 비교 상담이 각각 올바른 상품명으로 저장되는가
- 휴대전화 번호가 텍스트 형식으로 보존되는가
- 개인정보 미동의 시 브라우저에서 제출이 차단되는가
- 잘못된 상품 코드와 전화번호가 서버에서 거부되는가
- UTM 5종과 페이지 URL이 저장되는가
- 2분 안에 같은 연락처·상품을 재전송했을 때 중복 저장되지 않는가
- 모바일과 PC에서 성공·오류 문구가 보이는가
- 실제 배포 URL이 `/dev`가 아니라 `/exec`로 끝나는가
